version: 2.1

orbs:
  aws-cli: circleci/aws-cli@0.1.16

jobs:
  build:
    docker:
      - image: circleci/golang:latest
    steps:
      - checkout
      - aws-cli/install
      - deploy:
          command: |
            ./scripts/copy_to_s3.sh
