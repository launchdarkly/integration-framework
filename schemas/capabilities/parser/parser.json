{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "parser": {
    "$id": "#/parser",
    "title": "Response parser",
    "description": "Describes a mapping of property name to a location in the JSON response specified by a JSON pointer",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "id": {
        "$id": "#/properties/id",
        "title": "JSON Pointer to external condition ID",
        "type": "string"
      },
      "url": {
        "$id": "#/properties/url",
        "title": "JSON Pointer to external condition url",
        "type": "string"
      }
    },
    "required": ["id", "url"]
  }
}
