{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "internalConfigurationURL": {
    "$id": "#/properties/capability/internal-configuration-url",
    "title": "Internal Configuration URL",
    "description": "This capability will redirect users to an internal URL when they attempt to create or edit the integration from the integrations page in LaunchDarkly.",
    "type": "string",
    "maxLength": 2048,
    "pattern": "^(/[^/]+)+(?:\\?[^\\s]*)?$|^[^/]+(/[^/]+)*(?:\\?[^\\s]*)?$"
  }
}
