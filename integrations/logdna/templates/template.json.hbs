{
  "lines": [
    {
      "line": "{{{title.plainText}}}{{#if comment}}, Comment: {{comment}}{{/if}}",
      "app": "{{formVariables.app}}",
      "level": "INFO",
      "env": "{{project.environment.key}}"
    }
  ]
}
