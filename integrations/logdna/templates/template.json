{
  "lines": [
    {
      "line": "{{{titleObj.plainText}}}{{#if comment}}, Comment: {{comment}}{{/if}}",
      "app": "{{formVariables.app}}",
      "level": "INFO",
      "env": "{{project.environment.key}}"
    }
  ]
}
