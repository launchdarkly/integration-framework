{
  "lines": [
    {
      "line": "{{{title}}}{{#if comment}}, Comment: {{comment}}{{/if}}",
      "app": "{{formVariables.app}}",
      "level": "INFO",
      "env": "{{project.environment.key}}"
    }
  ]
}